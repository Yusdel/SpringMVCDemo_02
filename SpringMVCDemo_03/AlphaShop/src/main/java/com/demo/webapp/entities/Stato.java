package com.demo.webapp.entities;

/*
 * TODO New Notation :  Mapping Enumerator
 * Note. Since, if other elements were to be added to the enumerator,
 * the elements present would be out of phase, it's advisable to use enumType.STRING
 * and not the progressive number, enumType.ORDINAL
 */
public enum Stato 
{
	SOSPESO, // 0
	ATTIVO, // 1
}
